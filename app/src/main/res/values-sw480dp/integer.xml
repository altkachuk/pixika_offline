<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Catalog -->
    <integer name="catalog_columns_count">3</integer>

    <!-- Offers -->
    <integer name="offers_columns_count">3</integer>

    <!-- Product Search -->
    <integer name="product_search_columns_count">3</integer>

    <!-- Product Suggestions -->
    <integer name="product_suggestions_columns_count">3</integer>

    <!-- Seller Team -->
    <integer name="seller_team_columns_count">3</integer>

    <!-- Wishlist -->
    <integer name="wishlist_columns_count">3</integer>

    <!-- Sales -->
    <integer name="product_sales_columns_count">3</integer>

    <!-- Configuration -->
    <integer name="configuration_columns_count">2</integer>

    <!-- Payment Type -->
    <integer name="payment_type_count">2</integer>

</resources>