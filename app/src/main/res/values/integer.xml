<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- For R.anim Animations -->
    <integer name="transition_speed">300</integer>

    <!-- Catalog -->
    <integer name="catalog_columns_count">2</integer>

    <!-- Offers -->
    <integer name="offers_columns_count">2</integer>

    <!-- Product Search -->
    <integer name="product_search_columns_count">2</integer>

    <!-- Product Suggestions -->
    <integer name="product_suggestions_columns_count">2</integer>

    <!-- Seller Team -->
    <integer name="seller_team_columns_count">2</integer>

    <!-- Wishlist -->
    <integer name="wishlist_columns_count">2</integer>

    <!-- Sales -->
    <integer name="product_sales_columns_count">2</integer>

    <!-- Configuration -->
    <integer name="configuration_columns_count">1</integer>

    <!-- Payment Type -->
    <integer name="payment_type_count">1</integer>

    <!-- These are alias for the constants we'll reference below -->
    <integer name="view_visible">0</integer> <!-- This is VISIBLE -->
    <integer name="view_invisible">1</integer> <!-- This is INVISIBLE -->
    <integer name="view_gone">2</integer> <!-- This is GONE -->

    <integer name="self_signed_certificate">0</integer>
    <integer name="ca_certificate">1</integer>


</resources>